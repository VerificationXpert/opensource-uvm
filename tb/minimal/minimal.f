// Minimal testbench filelist.
//
// $TB_DIR is set by uvmake to this filelist's testbench directory, so the
// filelist does not need to know where the project lives.

+incdir+$TB_DIR

$TB_DIR/minimal_tb_top.sv
