# uvmake's cache: the fetched UVM checkout and the prebuilt shared libraries.
# Regenerable; see uvmake/core/uvm.mk. Override with UVMAKE_CACHE.
.uvmake/

# Per-testbench build output: verilated C++, objects, binaries, logs, waves
build/
*.vcd
*.fst
*.log

# Python bytecode from the self-tests
__pycache__/

# Editor / OS noise
*.swp
.DS_Store
